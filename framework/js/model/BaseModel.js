/*
 * BaseModel: Backbone NestedModel is responsibe to store nested data(hierarchical data). It uses Backbone Model.
 * 			  BaseModel is the base backbone model for all framework and module models.
 * 			  FrameworkModel and ModuleModel extends this class.
 * 		
 */
define(["nestedModel"],function(nestedModel){
	return Backbone.NestedModel.extend({
	});
});
