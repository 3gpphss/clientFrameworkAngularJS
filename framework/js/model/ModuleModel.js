/*
 * ModuleModel: Base model for all application module models. Extends from BaseModel.
 * 		
 * 'BaseModel': is the base backbone model for all framework and module models. 'BaseModel' is an alias whose 
 *  path is defined in require-config.js file.
 */
define(['BaseModel'],function(BaseModel){
	return BaseModel.extend({
	});
});


