/*
 * ModuleCollectionView: Collection view for modules. Extends BaseCollectionView.
 * 		
 */
define(['BaseCollectionView'],function(BaseCollectionView){
	return BaseCollectionView.extend({
	});
});
