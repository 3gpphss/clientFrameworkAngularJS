/*
 * BaseCollection: Backbone Collection is responsibe to store collections/lists of data.
 * 			  BaseCollection is the base backbone collection for all framework and module collections.
 * 		
 */
define(['backbone'],function(Backbone){
	return Backbone.Collection.extend({
	});
});
