/*
 * FrameworkView: Base view for all framework views. Extends from BaseView.
 * 		
 * 'BaseView': is the base backbone view for all framework and module views. 'BaseView' is an alias whose 
 *  path is defined in require-config.js file.
 */
define(['BaseView'],function(BaseView){
	return BaseView.extend({
	});
});


